export enum TransactionType {
  Sale = 'sale',
  Refund = 'refund',
  Void = 'void',
}

export enum TransactionStatus {
  Approved = 'approved',
  Pending = 'pending',
  Declined = 'declined',
  Cancelled = 'cancelled',
}
